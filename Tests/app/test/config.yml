imports:
    - { resource: database.php }

framework:
    fragments: ~
    secret: afasfsf
    translator:
        enabled: true

lexik_translation:
    fallback_locale: en
    managed_locales: [en, pl, de, sv]
