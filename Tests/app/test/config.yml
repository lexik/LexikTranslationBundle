imports:
    - { resource: database.php }

framework:
    fragments: ~
    secret: afasfsf
    default_locale: 'en'
    enabled_locales: ['en', 'pl', 'de', 'sv']
    translator:
        enabled: true
        fallbacks: ['en']

#lexik_translation:
#    fallback_locale: en
#    managed_locales: [en, pl, de, sv]
